Restyle my existing dashboard (attached separately) into a neo-brutalism design system, using the visual style of the reference screenshot exactly — but keep my dashboard's own layout, structure, and content unchanged (same sidebar items, same cards, same sections, same data).

Match the reference design 1:1 on these specific attributes:

Corners: Zero border-radius on every single element — sidebar, cards, buttons, search bar, avatars' containers, stat boxes, dropdowns, icon boxes. No rounded corners anywhere, no exceptions.
Borders: Every card, button, input, and container gets a solid black border, 2–3px thick, flat (no blur/soft shadow). Where the reference uses layered borders (card inside a bordered section), replicate that same nested-border look.
Color system: Use flat, saturated block colors exactly like the reference — assign one dominant flat color per section/card (e.g. mustard yellow, sage/mint green, soft pink, cream/off-white), with black text and black borders on top. No gradients, no soft pastLighting, no drop shadows — colors sit as solid flat blocks the same way the reference uses yellow for the trainer card, pink for stats, cream for the sidebar/search.
Buttons: Two button styles only, matching the reference exactly:
Primary: solid black fill, white bold text, black border, sharp corners
Secondary: white/cream fill, black bold text, black border, sharp corners
No color buttons other than these two, no shadow-press effects unless the reference shows one.
Typography: Headings in a bold monospace/technical font (like the reference's "Trainer info" style — JetBrains Mono, Space Mono, or IBM Plex Mono, bold weight). Body text stays in a clean sans-serif for readability. Match the reference's heading weight and letter spacing exactly.
Icons/avatars: Small icon containers as black-background-white-icon squares (no circles, no radius), same as the reference's icon boxes. Profile avatars can stay circular only if the reference shows circular avatars — otherwise square with black border.
Graphs/charts: Any line or bar chart on my dashboard should use the same flat, hand-drawn/solid-stroke look as the reference's activity graph — solid black or single-color stroke, no gradient fill, no smoothing curves, minimal and raw.
Outer frame: If my dashboard has a container/frame, apply the same soft pastel diagonal background behind a hard-bordered main container, like the reference.

Do not change: my dashboard's existing layout, section order, number of cards, sidebar navigation items, or content/copy — only the colors, border style, corner radius, button style, font style, icon style, and graph styling should change to match the reference exactly.